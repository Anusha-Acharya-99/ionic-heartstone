import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anusha.heartstonelib',
  appName: 'heart-stone-lib',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
